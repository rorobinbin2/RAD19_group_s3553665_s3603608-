<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 100 %>
    </li>
    <h2>
      Coordinator name:
      <%= link_to user.name, user %>
    </h2>
  <% end %>
</ul>
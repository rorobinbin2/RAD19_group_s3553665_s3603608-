        
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
             Location <b class="caret"></b>
          </a>
          
          <ul class="dropdown-menu">
            <% @locations.each do |location| %>
              <li><%= link_to location.name, '#' %></li> 
            <% end %>
            <li class="divider"></li>
              <li><%= link_to "Create location", addlocation_path %></li>
          </ul>  
        </li> 